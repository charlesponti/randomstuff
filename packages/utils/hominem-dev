#!/usr/bin/env bash

# Get the directory where the script is located
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"

# Construct path to cli.ts relative to script location
CLI_PATH="${SCRIPT_DIR}/src/google/cli.ts"

# Run the CLI with all passed arguments
bun run "$CLI_PATH" "$@"

